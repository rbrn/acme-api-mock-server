# WireMock Files Directory

This directory is used for storing static response files that can be referenced in WireMock mappings using the `bodyFileName` property.

Currently, responses are defined inline in the mapping files using `jsonBody`.
